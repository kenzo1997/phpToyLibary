version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: toyframework_db
    restart: always
    environment:
      MYSQL_DATABASE: toyframework
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql/

volumes:
  mysql_data:
